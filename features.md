The Indexer primitive is as follows: 
    - Upload a knowledge base 
    - Use it to power semantic search, RAG, or instant query it. 
    - Allow reuse of the same knowledge base across different notebooks. 
